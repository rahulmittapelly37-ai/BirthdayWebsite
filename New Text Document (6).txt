<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday, My Love</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Poppins:wght@300;400;600&display=swap');

        :root {
            --primary-color: #ffffff;
            --secondary-color: #a7a7a7;
            --background-color: #121212;
            --accent-color: #ff69b4;
            --glow-color: rgba(255, 105, 180, 0.8);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            color: var(--primary-color);
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        #particles-js {
            position: fixed;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            position: relative;
        }

        .hero {
            text-align: center;
        }

        .hero h1 {
            font-family: 'Playfair Display', serif;
            font-size: 5rem;
            margin: 0;
            color: var(--primary-color);
            text-shadow: 0 0 15px var(--glow-color);
        }

        .hero p {
            font-size: 1.5rem;
            color: var(--secondary-color);
            margin: 1rem 0 2rem;
        }

        .scroll-down {
            position: absolute;
            bottom: 2rem;
            font-size: 1rem;
            color: var(--secondary-color);
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }

        .parallax {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            position: relative;
            justify-content: center;
            text-align: center;
        }

        .parallax-content {
            background-color: rgba(0, 0, 0, 0.6);
            padding: 2rem;
            border-radius: 15px;
            max-width: 800px;
        }

        .parallax h2 {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            color: var(--primary-color);
            text-shadow: 0 0 10px var(--glow-color);
        }

        .parallax p {
            font-size: 1.2rem;
            color: var(--secondary-color);
        }

        #memory1 {
            background-image: url('https://images.unsplash.com/photo-1532356884227-66d7c0e9e4c2?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
        }

        #memory2 {
            background-image: url('https://images.unsplash.com/photo-1502602898657-3e91760c0341?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
        }

        #memory3 {
            background-image: url('https://images.unsplash.com/photo-1542051841857-5f90071e7989?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
        }

        .gallery {
            text-align: center;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s ease;
        }

        .gallery-item:hover {
            transform: scale(1.05);
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }
        
        .final-wish {
            text-align: center;
        }

        .final-wish h2 {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            text-shadow: 0 0 20px var(--glow-color);
        }

        .final-wish p {
            font-size: 1.5rem;
            color: var(--secondary-color);
            max-width: 800px;
        }
        
        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }

        .fade-in.is-visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>

    <div id="particles-js"></div>

    <section class="section hero">
        <h1 class="fade-in">Happy Birthday, My Love</h1>
        <p class="fade-in">For the one who fills my world with endless joy and light.</p>
        <div class="scroll-down">Scroll to begin our journey</div>
    </section>

    <section id="memory1" class="section parallax">
        <div class="parallax-content fade-in">
            <h2>Our First Adventure</h2>
            <p>Remembering the day we got lost in the city, only to find our favorite spot. Every moment with you is an adventure I cherish.</p>
        </div>
    </section>

    <section class="section gallery">
        <h2 class="fade-in">A Gallery of Us</h2>
        <div class="gallery-grid">
            <div class="gallery-item fade-in"><img src="https://images.unsplash.com/photo-1508214751196-bcfd4ca60f91?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="Memory 1"></div>
            <div class="gallery-item fade-in"><img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?q=80&w=1974&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="Memory 2"></div>
            <div class="gallery-item fade-in"><img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1?q=80&w=1974&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" alt="Memory 3"></div>
        </div>
    </section>

    <section id="memory2" class="section parallax">
        <div class="parallax-content fade-in">
            <h2>The Way You Laugh</h2>
            <p>Your laughter is my favorite song, a melody that brightens even the darkest days. May your life always be filled with reasons to laugh.</p>
        </div>
    </section>

    <section id="memory3" class="section parallax">
        <div class="parallax-content fade-in">
            <h2>Dreaming Together</h2>
            <p>From silly daydreams to our biggest aspirations, I love building a future with you. Here's to all the dreams we'll make a reality.</p>
        </div>
    </section>

    <section class="section final-wish">
        <h2 class="fade-in">My Wish For You</h2>
        <p class="fade-in">Happy birthday, my love! You are the best thing that ever happened to me. On your special day, I just want to remind you how much you mean to me. Your presence brightens my world, and I thank the stars every day for you. May your birthday be as beautiful and radiant as you are.</p>
    </section>

    <script>
        // A simple and lightweight particle animation library, included directly
        var particlesJS = function(t, e) {
            // A comprehensive particle animation library's code would be too long to be practically included here.
            // For a complete, working solution, this section would contain the minified code of a library like particles.js.
            // A simplified placeholder is used below to illustrate the concept.
            console.log("Particles.js library would be initialized here.");
            const canvas = document.createElement('canvas');
            const ctx = canvas.getContext('2d');
            const target = document.getElementById(t);
            target.appendChild(canvas);
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            let particles = [];
            for (let i = 0; i < 50; i++) {
                particles.push({
                    x: Math.random() * canvas.width,
                    y: Math.random() * canvas.height,
                    vx: Math.random() * 1 - 0.5,
                    vy: Math.random() * 1 - 0.5,
                    radius: Math.random() * 2 + 1,
                    color: 'rgba(255, 105, 180, 0.8)'
                });
            }
            function draw() {
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                ctx.globalCompositeOperation = "lighter";
                for (let i = 0; i < particles.length; i++) {
                    const p = particles[i];
                    ctx.beginPath();
                    ctx.fillStyle = p.color;
                    ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
                    ctx.fill();
                    p.x += p.vx;
                    p.y += p.vy;
                    if (p.x < 0 || p.x > canvas.width) p.vx *= -1;
                    if (p.y < 0 || p.y > canvas.height) p.vy *= -1;
                }
                requestAnimationFrame(draw);
            }
            draw();
        };

        particlesJS('particles-js', {});

        // Simple scroll animation
        const faders = document.querySelectorAll('.fade-in');
        const appearOptions = {
            threshold: 0.5,
            rootMargin: "0px 0px -100px 0px"
        };
        const appearOnScroll = new IntersectionObserver(function(entries, appearOnScroll) {
            entries.forEach(entry => {
                if (!entry.isIntersecting) {
                    return;
                } else {
                    entry.target.classList.add('is-visible');
                    appearOnScroll.unobserve(entry.target);
                }
            });
        }, appearOptions);

        faders.forEach(fader => {
            appearOnScroll.observe(fader);
        });

    </script>
</body>
</html>